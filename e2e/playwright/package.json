{
  "name": "world-builder-e2e",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "playwright test --config=playwright.config.ts",
    "install-browser-drivers": "npx playwright install"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
